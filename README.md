Amazon AWS EC2 Service

# 패키지 리스트 업데이트
sudo apt-get update

# 레포지토리 추가
sudo add-apt-repository ppa:openjdk-r/ppa

# openjdk 설치
sudo apt-get install openjdk-17-jdk




sudo docker run -d --name my-web -p 80:8080 ximonk/sb-docker-3:latest
